Вот инструкция, как "законсервировать" этот канал и как быстро его расконсервировать, когда Cloudflare упадет.

### Как выключить всё прямо сейчас

Вам нужно сделать две вещи: перестать транслировать порт в интернет (Tailscale) и остановить программу авторизации (Docker), чтобы не ела память.

1.  **Выключаем доступ из интернета (Самое важное):**
    Эта команда полностью удаляет настройки проброса портов.
    ```bash
    sudo tailscale serve --reset
    ```
    *После этого ссылка `https://ваша-нода...ts.net` перестанет открываться.*

2.  **Останавливаем контейнер OAuth2-Proxy:**
    ```bash
    cd /opt/auth-proxy
    sudo docker-compose stop
    ```
    *Мы используем `stop`, а не `down`, чтобы контейнер просто "уснул", но не удалился. Так он запустится быстрее в следующий раз.*

Всё. Теперь система спит. Сервисы OpenWebUI работают как обычно, но доступ к ним только локальный (или через Cloudflare).

---

### 🚨 "Тревожная кнопка": Как включить, когда всё упало


Когда нужно срочно восстановить доступ:

1.  **Зайдите на сервер по SSH.**

2.  **Запустите прокси:**
    ```bash
    cd /opt/auth-proxy
    sudo docker-compose start
    ```

3.  **Откройте шлюз в интернет:**
    ```bash
    sudo tailscale funnel --bg --https=443 http://localhost:4180
    ```

Через 10-15 секунд ваш резервный адрес снова будет доступен с Google-авторизацией.
